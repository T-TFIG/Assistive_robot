# 🔄 Iterative Closest Point (ICP)

The Iterative Closest Point (ICP) algorithm is used in this project to align two 3D point clouds. It estimates the rigid transformation (rotation and translation) required to match a moving point cloud to a fixed reference.